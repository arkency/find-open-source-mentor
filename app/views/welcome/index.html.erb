<h1>A platform to connect junior developers with open source projects</h1>
<p>First task: make this app work:)</p>
<div>
  <ul>
    <li><%= link_to "Available junior developers", juniors_path %></li>
    <li><%= link_to "Open-source projects with mentoring available", projects_path %></li>
  </ul>
</div>

<div>
  <% if user_signed_in? %>
    <%= link_to 'Sign Out', destroy_user_session_path, method: :delete %>
  <% else %>
   <p><%= link_to "Sign in as Mentor with Github ", user_github_omniauth_authorize_path(role: 'mentor') %></p>
    <p><%= link_to "Sign in as Mentee with Github", user_github_omniauth_authorize_path(role: 'mentee') %></p>
  <% end %>
</div>

<div>
  <% if user_signed_in? %>
    <%= button_to 'Add repository', new_repository_path, method: :get %>
  <% end %>
</div>
